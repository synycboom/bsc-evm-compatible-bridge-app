import styled from 'styled-components';

const TransferNFTStyle = styled.div`
  .progress-container {
    margin-top: 16px;
    display: flex;
    align-items: center;

    .status-label {
      font-size: 18px;
      margin-right: 8px;
      font-weight: bold;
    }
    .ant-tag {
      font-size: 16px;
      font-size: 14px;
      font-weight: bold;
      line-height: 22px;
    }
  }
`;

export default TransferNFTStyle;
